{
  "name": "@acme/auth",
  "private": true,
  "type": "module",
  "exports": {
    "./*": "./src/*.ts"
  },
  "scripts": {
    "build": "tsdown --clean",
    "clean": "rm -rf dist node_modules",
    "test": "vitest run"
  },
  "dependencies": {
    "@acme/db": "workspace:*",
    "@acme/env": "workspace:*",
    "@better-auth/drizzle-adapter": "catalog:",
    "better-auth": "catalog:",
    "drizzle-orm": "catalog:",
    "effect": "catalog:",
    "pg": "catalog:"
  },
  "devDependencies": {
    "@effect/vitest": "catalog:",
    "@types/node": "catalog:",
    "@types/pg": "catalog:",
    "tsdown": "catalog:",
    "vitest": "catalog:"
  }
}
